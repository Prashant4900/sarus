name: src
description: A new jaspr project.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dart_frog: ^1.2.0
  jaspr: ^0.17.1

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.1.1
  jaspr_builder: ^0.17.1
  jaspr_lints: ^0.2.1
  lints: ^3.0.0

jaspr:
  mode: server
  dev-command: dart_frog dev
